[project]
name = "crovia-spider"
version = "0.1.0"
description = "Crovia Spider — turn open training corpora into spider_receipt.v1 logs."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
  { name = "Crovia Trust", email = "info@croviatrust.com" }
]

dependencies = [
  "pandas>=2.1.0",
  "pyarrow>=15.0.0",
]

[project.scripts]
crovia-spider = "crovia_spider.cli:main"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"
